rule = ^ _ :expr _ $ { expr };

expr =
  | addition
  | primary;

@inline addition = :expr _ "+" _ :primary { expr + primary };

@inline primary =
  | "(" _ :expr _ ")" { expr }
  | \d+ { int.parse(span) };

_ = \s*;
